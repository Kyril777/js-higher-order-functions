/* Accepts a function as an argument. */

const array  = [1, 2, 4];
const doubler = array.map(function mapper(number) {
  return 2 * number;
});



let array = ['4', '8', '12'];
let arrayDoubled = [];
for (let i = 0; i < array.length; i++) {
  arrayDoubled.push(arrau[i] * 2);
