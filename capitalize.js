/* Use map() function to capitalize each word.*/ 

const words = ["drive", "me", "crazy"];
const capitalized = words.map((word) => word.toUpperCase());

let fruits = ["apple", "orange", "cherry"
]

/* Another method. */
const names = ['David', 'James', 'Beko'].map(name => name.toUpperCase());

/* Additional method. */
let array = ['hammer', 'wrench', 'screwdriver'];
let capitalize = array.map(function(x) { return x.toUpperCase(); });
console.log(capitalize);

